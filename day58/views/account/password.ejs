<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.4.1/font/bootstrap-icons.css"
    />
  <link
    rel="stylesheet"
    href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.2/css/bootstrap.min.css"
    integrity="sha512-b2QcS5SsA8tZodcDtGRELiGv5SaKSk1vDHDaQRda0htPYWZ6046lr3kJ5bAAQdpV2mmA/4v0wQF9MyU6/pDIAg=="
    crossorigin="anonymous"
    referrerpolicy="no-referrer"
  />
  <title>Edit Account</title>
</head>
<body>
  <div class="py-3">
    <div class="container">
      <h2>Đổi mật khẩu</h2>
  <form action="" method="post">
    <div class="mb-3">
      <label for="">Mật khẩu cũ</label>
      <input
        type="password"
        name="passwordOld"
        value="<%= body?.passwordOld %>"
        class="form-control"
        placeholder="Mật khẩu ..."
      />
      <span class="text-danger" ><%- validate.getError(errors,"passwordOld") %></span >
      <%- wrong.length ? `<div class="text-danger">${wrong[0]}</div>`: '' %>
    </div>
    <div class="mb-3">
      <label for="">Mật khẩu mới</label>
      <input
        type="password"
        name="passwordNew"
        value="<%= body?.passwordNew %>"
        class="form-control"
        placeholder="Mật khẩu ..."
      />
      <span class="text-danger" ><%- validate.getError(errors,"passwordNew") %></span >
    </div>
    <div class="mb-3">
      <label for="">Nhập lại mật khẩu mới</label>
      <input
        type="password"
        value="<%= body?.rePasswordNew %>"
        name="rePasswordNew"
        class="form-control"
        placeholder="Mật khẩu ..."
      />
      <span class="text-danger" ><%- validate.getError(errors,"rePasswordNew") %></span >
      <%- wrongRe.length ? `<div class="text-danger">${wrongRe[0]}</div>`: '' %>
    </div>
    <button type="submit" class="btn btn-success">Lưu</button>
    <a href="/" class="btn btn-danger">Hủy</a>
  </form>
    </div>
  </div>
</body>
</html>
